os: osx
osx_image: xcode10
before_install:
  - brew update
#language: python
#python:
#  - "3.6"
# command to install dependencies
#install:
#  - pip install -r requirements.txt
# command to run tests
script:
  - echo "hello world"
  - aws s3 ls s3://travis-python-archives/binaries/osx/10.13/x86_64/
